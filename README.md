
```bash
pip install -r requirements.txt
python run.py
```

## Steps

1. Choose a pipeline:
   - `1` = YOLO bbox + depth
   - `2` = YOLO seg + bbox + depth
2. Enter the image number (e.g., `1` -> `1.jpg`).
3. Choose an option from the menu.
